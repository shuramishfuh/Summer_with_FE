@model Summer.Models.TestScore

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>TestScore</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Course.CourseName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Course.CourseName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Student.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Student.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.StudentScore)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.StudentScore)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Comment)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Comment)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.DateTime)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.DateTime)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color" id="form">
            <input type="button" value="Delete" class="btn btn-default btn-danger" id="Btn" /> |
            @Html.ActionLink("Back to List", "Index")
        </div>
    }
</div>
@section scripts

{
    <script>

        $("#Btn").on('click', function() {



            bootbox.confirm({
                    message: "Do U confirm? This cannot be undone",
                    buttons: {
                        confirm: {
                            label: 'Confirm',
                            className: 'btn-lg btn-danger'
                        },
                        cancel: {
                            label: 'Reject',
                            className: 'btn-lg  btn-info'
                        }
                    },
                    callback: function (result) {
                        if (result)
                            $("form").submit();
                    }
                }

            );
        });


    </script>
}