@model Summer.Models.ApplicationUser

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>myUser</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>


        <dt>
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.UserName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.UserName)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color" id="form">
            <input type="button" value="Delete" class="btn btn-danger " id="Btn" /> |
            @Html.ActionLink("Back to List", "ListUsers")
        </div>
    }
</div>
@section scripts

{
    <script>

        $("#Btn").on('click', function() {



            bootbox.confirm({
                    message: "Do U confirm? This cannot be undone",
                    buttons: {
                        confirm: {
                            label: 'Confirm',
                            className: 'btn-lg btn-danger'
                        },
                        cancel: {
                            label: 'Reject',
                            className: 'btn-lg  btn-info'
                        }
                    },
                    callback: function (result) {
                        if (result)
                            $("form").submit();
                    }
                }

            );
        });


    </script>
}